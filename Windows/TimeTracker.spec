# -*- mode: python ; coding: utf-8 -*-

from pathlib import Path

base_dir = Path(SPECPATH).resolve()


a = Analysis(
    [str(base_dir / 'main.py')],
    pathex=[str(base_dir)],
    binaries=[],
    datas=[(str(base_dir / 'icon.png'), '.')],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.datas,
    [],
    name='TimeTracker',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=False,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)
